import React from 'react'
import { carType } from '../utils/types'


type props = {
    car: carType;
}

const VehicleCard = ({ car }: React.PropsWithChildren<props>) => {
    return (
        <>
        </>
    )
}

export default VehicleCard
